# Lab 2 · ฝึก SQL ขั้นสูง (JOIN / GROUP BY / Transaction)

## เป้าหมาย
ฝึกเชื่อมตารางด้วย JOIN (รวมความสัมพันธ์ Many-to-Many), สรุปข้อมูลด้วย GROUP BY และทดลอง Transaction จริง

## สิ่งที่ต้องเตรียม
1. phpMyAdmin → ฐานข้อมูล `atsoft_day1`
2. Import [`sql/02_advanced_seed.sql`](./sql/02_advanced_seed.sql) → ได้ตาราง `products`, `customers`, `orders`, `accounts`, `order_items`
   (ไฟล์นี้สร้างตาราง `products` ให้ในตัว ไม่ต้อง import basic ก่อน)
3. ตรวจว่ามีสินค้า 10 รายการ, ลูกค้า 4 คน, คำสั่งซื้อ 6 รายการ, รายการในคำสั่งซื้อ (order_items) 9 แถว, บัญชี 2 บัญชี

เขียนคำตอบในช่องว่าง แล้วเทียบ [เฉลย Lab 2](./answers/04_lab_sql_advanced_answer.md)

---

## ส่วนที่ 1: JOIN

**ข้อ 1.1** แสดง `ชื่อลูกค้า`, `สินค้า`, `จำนวน` ของทุกคำสั่งซื้อ (ใช้ INNER JOIN)
```sql

```

**ข้อ 1.2** แสดงคำสั่งซื้อทั้งหมดของลูกค้าชื่อ `มานี รักเรียน`
> JOIN แล้วใส่ `WHERE c.name = '...'`
```sql

```

**ข้อ 1.3** แสดง **ลูกค้าทุกคน** พร้อมสินค้าที่สั่ง — รวมคนที่ยังไม่เคยสั่งด้วย (ใช้ LEFT JOIN)
> ลูกค้าที่ไม่เคยสั่งจะเห็น product เป็น NULL
```sql

```

---

## ส่วนที่ 2: Aggregate + GROUP BY

**ข้อ 2.1** นับว่ามีคำสั่งซื้อทั้งหมดกี่รายการ
```sql

```

**ข้อ 2.2** หาว่าลูกค้าแต่ละคนสั่งสินค้า **รวมกี่ชิ้น** (แสดงชื่อลูกค้า + ยอดรวม)
> `GROUP BY c.name` แล้ว `SUM(o.amount)`
```sql

```

**ข้อ 2.3** คำนวณ **ยอดเงินรวม** ของแต่ละคำสั่งซื้อ (จำนวน × ราคา) และแสดงคู่กับชื่อลูกค้า
> `SELECT c.name, o.product, (o.amount * o.price) AS total ...`
```sql

```

**ข้อ 2.4** หายอดเงินรวมที่ลูกค้าแต่ละคนใช้จ่ายทั้งหมด เรียงจากมากไปน้อย
> `SUM(o.amount * o.price)` + `GROUP BY` + `ORDER BY ... DESC`
```sql

```

**ข้อ 2.5** แสดงเฉพาะลูกค้าที่ใช้จ่ายรวม **เกิน 100 บาท** (ใช้ HAVING)
```sql

```

---

## ส่วนที่ 3: Transaction (ฝึกบนตาราง accounts)

ดูยอดเงินตั้งต้นก่อน:
```sql
SELECT * FROM accounts;
-- บัญชี A = 1000, บัญชี B = 500
```

**ข้อ 3.1** จำลองการโอนเงิน **200 บาท จากบัญชี A (id=1) ไปบัญชี B (id=2)** ด้วย Transaction แบบยืนยัน (COMMIT)
```sql

```
ตรวจผล: A ควรเหลือ 800, B ควรเป็น 700

**ข้อ 3.2** ทดลองโอนอีก 9999 บาทจาก A ไป B แต่ครั้งนี้ให้ **ROLLBACK** (ยกเลิก) แล้วเช็คว่ายอดเงินไม่เปลี่ยน
> ทำ START TRANSACTION → UPDATE 2 บรรทัด → SELECT ดูยอด (จะเห็นยอดเปลี่ยนชั่วคราว) → ROLLBACK → SELECT อีกครั้ง (ยอดกลับเหมือนเดิม)
```sql

```

**ข้อ 3.3** จำลองการขายจริงด้วย Transaction: ลูกค้า id=2 ซื้อ `น้ำดื่ม 600ml` 3 ชิ้น
ให้ทำ 2 อย่างใน Transaction เดียว แล้ว **COMMIT**: (1) หักสต็อกในตาราง `products` ลง 3 และ (2) เพิ่มแถวใน `orders`
> `UPDATE products SET stock = stock - 3 WHERE name = 'น้ำดื่ม 600ml';` + `INSERT INTO orders (...) VALUES (...)`
```sql

```

**ข้อ 3.4** (SAVEPOINT) เริ่ม Transaction → ถอนเงิน A ออก 100 → ตั้ง `SAVEPOINT s1` → เพิ่มเงิน B 100 →
สั่ง `ROLLBACK TO s1` (ยกเลิกเฉพาะการเพิ่มเงิน B) → `COMMIT` แล้วเช็คว่า A ลดแต่ B เท่าเดิม
```sql

```

---

## ส่วนที่ 4: Many-to-Many (ตาราง order_items)

ความสัมพันธ์ M:N: 1 คำสั่งซื้อมีหลายสินค้า / 1 สินค้าอยู่ในหลายคำสั่งซื้อ เชื่อมผ่านตารางกลาง `order_items`

**ข้อ 4.1** แสดงรายการสินค้าในแต่ละคำสั่งซื้อ: `order_id`, `ชื่อสินค้า`, `จำนวน` (JOIN 3 ตาราง: orders → order_items → products)
```sql

```

**ข้อ 4.2** คำสั่งซื้อใบที่ 1 (`order_id = 1`) มีสินค้าอะไรบ้าง กี่ชิ้น
```sql

```

**ข้อ 4.3** คำนวณ **ยอดเงินรวมของแต่ละคำสั่งซื้อ** = ผลรวมของ (จำนวน × ราคาต่อชิ้น)
> JOIN 3 ตาราง + `SUM(oi.quantity * p.price)` + `GROUP BY o.id`
```sql

```

**ข้อ 4.4** สินค้าแต่ละชิ้นถูกสั่งซื้อรวมกี่ชิ้น (จากทุกคำสั่งซื้อ) เรียงมากไปน้อย
> `GROUP BY p.name` + `SUM(oi.quantity)`
```sql

```

---

## โจทย์ท้าทาย
**ข้อ 5.1** หาว่าสินค้าใดถูกสั่งซื้อรวม (amount) มากที่สุด
> `GROUP BY product` + `ORDER BY SUM(amount) DESC LIMIT 1`
```sql

```

**ข้อ 5.2** นับจำนวนคำสั่งซื้อของลูกค้าแต่ละคน รวมถึงคนที่มี 0 รายการ
> LEFT JOIN + `COUNT(o.id)` (นับ o.id เพื่อให้คนไม่มี order ได้ 0)
```sql

```

---

## เช็คความเข้าใจ
- [ ] เขียน INNER JOIN และ LEFT JOIN ได้ และเข้าใจความต่าง
- [ ] JOIN 3 ตารางผ่านตารางกลาง (M:N) ได้
- [ ] ใช้ GROUP BY + SUM/COUNT สรุปข้อมูลได้
- [ ] ใช้ HAVING กรองผลที่สรุปแล้วได้
- [ ] เข้าใจว่า COMMIT ยืนยัน, ROLLBACK ยกเลิก และใช้ SAVEPOINT ย้อนบางส่วนได้
