# Lab 1 · ฝึก SQL พื้นฐาน (CRUD)

## เป้าหมาย
ฝึกเขียน `INSERT / SELECT / UPDATE / DELETE` พร้อม `WHERE`, `LIKE`, `ORDER BY`, `LIMIT` บนตารางสินค้าจริง

## สิ่งที่ต้องเตรียม
1. เปิด phpMyAdmin → เลือกฐานข้อมูล `atsoft_day1`
2. นำเข้าข้อมูลตั้งต้น: แท็บ **Import** → เลือกไฟล์ [`src/01_basic_seed.sql`](./src/01_basic_seed.sql) → **Go**
   (หรือเปิดไฟล์ คัดลอกเนื้อหาไปวางในแท็บ **SQL** แล้ว Go)
3. ตรวจว่ามีตาราง `products` พร้อมข้อมูล 10 แถว

> วิธีทำ Lab: อ่านโจทย์ → เขียนคำสั่ง SQL ในแท็บ SQL → กด Go → ดูผลลัพธ์
> เขียนคำตอบของคุณไว้ในช่องว่างใต้แต่ละข้อ แล้วค่อยเทียบ [เฉลย](./answers/02_lab_sql_basic_answer.md)

---

## ส่วนที่ 1: READ (SELECT) — อ่านและค้นหา

**ข้อ 1.1** แสดงสินค้าทั้งหมด (ทุกคอลัมน์)
```sql
-- เขียนคำตอบที่นี่

```

**ข้อ 1.2** แสดงเฉพาะคอลัมน์ `name` และ `price` ของทุกสินค้า
```sql

```

**ข้อ 1.3** แสดงสินค้าที่อยู่ในหมวด `เครื่องดื่ม`
```sql

```

**ข้อ 1.4** แสดงสินค้าที่ราคามากกว่า 20 บาท
```sql

```

**ข้อ 1.5** แสดงสินค้าที่ **สต็อกน้อยกว่า 50** (ของใกล้หมด)
```sql

```

**ข้อ 1.6** ค้นหาสินค้าที่ชื่อมีคำว่า `น้ำ`
```sql

```

**ข้อ 1.7** แสดงสินค้าทั้งหมด เรียงตามราคาจาก **มากไปน้อย**
```sql

```

**ข้อ 1.8** แสดงสินค้าที่ราคาแพงที่สุด **3 อันดับแรก**
```sql

```

**ข้อ 1.9** แสดงสินค้าที่ราคาอยู่ระหว่าง 10 ถึง 50 บาท (ใช้ `BETWEEN`)
```sql

```

---

## ส่วนที่ 2: CREATE (INSERT) — เพิ่มข้อมูล

**ข้อ 2.1** เพิ่มสินค้าใหม่ 1 ชิ้น: ชื่อ `แก้วพลาสติก`, หมวด `บรรจุภัณฑ์`, ราคา `2.50`, สต็อก `1000`
```sql

```

**ข้อ 2.2** เพิ่มสินค้าพร้อมกัน 2 ชิ้นในคำสั่งเดียว (คิดชื่อ/ราคาเองได้)
```sql

```

**ข้อ 2.3** SELECT ดูอีกครั้ง ว่าสินค้าที่เพิ่งเพิ่มเข้าไปอยู่ครบไหม
```sql

```

---

## ส่วนที่ 3: UPDATE — แก้ไขข้อมูล

**ข้อ 3.1** สินค้า `หมวกนิรภัย` ขึ้นราคาเป็น `280.00` — แก้ให้ถูกต้อง
> ใช้ `WHERE name = '...'` หรือดู id จากตารางก่อน
```sql

```

**ข้อ 3.2** เพิ่มสต็อกของ `น้ำดื่ม 600ml` อีก 200 ชิ้น (จากเดิม 500 เป็น 700)
> ทำได้ 2 แบบ: ใส่ค่าใหม่ตรงๆ (`SET stock = 700`) หรือใช้ `SET stock = stock + 200`
```sql

```

**ข้อ 3.3** ลดราคาสินค้าทุกชิ้นในหมวด `อุปกรณ์` ลง 10% (ราคาใหม่ = ราคาเดิม × 0.9)
```sql

```

---

## ส่วนที่ 4: DELETE — ลบข้อมูล

**ข้อ 4.1** ลบสินค้าที่ชื่อ `เทปกาวใส`
```sql

```

**ข้อ 4.2** ลบสินค้าทั้งหมดที่สต็อกเป็น 0 (ถ้ามี)
```sql

```

---

## โจทย์ท้าทาย (ทำถ้าเหลือเวลา)
**ข้อ 5.1** หาสินค้าหมวด `อุปกรณ์` ที่ราคาแพงที่สุด 2 อันดับแรก
> รวม `WHERE` + `ORDER BY ... DESC` + `LIMIT` เข้าด้วยกัน
```sql

```

**ข้อ 5.2** นับว่ามีสินค้าในหมวด `อุปกรณ์` กี่ชิ้น
> ใช้ `SELECT COUNT(*) ...` (เป็นการ preview เนื้อหาชีทถัดไป)
```sql

```

---

## เช็คความเข้าใจ
- [ ] เขียน SELECT + WHERE + ORDER BY + LIMIT ได้
- [ ] เพิ่มข้อมูลด้วย INSERT ได้ทั้งทีละแถวและหลายแถว
- [ ] แก้ข้อมูลด้วย UPDATE โดยใส่ WHERE ทุกครั้ง
- [ ] ลบข้อมูลด้วย DELETE อย่างระมัดระวัง
